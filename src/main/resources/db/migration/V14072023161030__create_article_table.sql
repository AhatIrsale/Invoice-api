CREATE TABLE Article (
                         id UUID PRIMARY KEY,
                         description VARCHAR(255),
                         price DECIMAL(10, 2),
                         quantite INT
);
