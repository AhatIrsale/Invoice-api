CREATE TABLE Personne (
                          id UUID PRIMARY KEY,
                          name VARCHAR(255),
                          mail VARCHAR(255),
                          password VARCHAR(255),
                          type VARCHAR(255)
);
